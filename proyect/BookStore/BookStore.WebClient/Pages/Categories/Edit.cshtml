@page
@model BookStore.WebClient.Pages.Categories.EditModel

@{
    ViewData["Title"] = "Edit Categories";
}

<h1>Edit Categories</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Categories.Id" />
            <div class="form-group">
                <label asp-for="Categories.Name" class="control-label"></label>
                <input asp-for="Categories.Name" class="form-control" />
                <span asp-validation-for="Categories.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
